version: 2.1

jobs:
  lint_and_test:
    steps:
      - run:
          name: Runs ESLint on the JavaScript code
          command: npm run test


workflows:
  commit-workflo:
    jobs:
      - lint_and_test